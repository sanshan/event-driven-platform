import { readHandlerResolver } from './read-handler-resolver.js';

describe('readHandlerResolver', () => {
    it('should work', () => {
        expect(readHandlerResolver()).toEqual('read-handler-resolver');
    });
});
