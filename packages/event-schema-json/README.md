# event-schema-json

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build event-schema-json` to build the library.

## Running unit tests

Run `nx test event-schema-json` to execute the unit tests via [Vitest](https://vitest.dev/).
