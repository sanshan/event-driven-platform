import { useCaseExecutor } from './use-case-executor.js';

describe('useCaseExecutor', () => {
    it('should work', () => {
        expect(useCaseExecutor()).toEqual('use-case-executor');
    });
});
