{
    "name": "@event-driven-platform/reader",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/reader/src",
    "projectType": "library",
    "tags": [],
    "// targets": "to see all targets run: nx show project @event-driven-platform/reader --web",
    "targets": {
        "test:verification": {
            "executor": "nx:run-commands",
            "cache": false,
            "options": {
                "command": "vitest run --config vitest.verification.config.mts",
                "cwd": "packages/reader"
            }
        }
    }
}
