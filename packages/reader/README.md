# reader

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build reader` to build the library.

## Running unit tests

Run `nx test reader` to execute the unit tests via [Vitest](https://vitest.dev/).
