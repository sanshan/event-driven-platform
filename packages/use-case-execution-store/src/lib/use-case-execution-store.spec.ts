import { useCaseExecutionStore } from './use-case-execution-store.js';

describe('useCaseExecutionStore', () => {
    it('should work', () => {
        expect(useCaseExecutionStore()).toEqual('use-case-execution-store');
    });
});
